/** Mirrors OneDev BrandLogoPanel — custom site logo or default SVG icon. */
export function BrandLogo() {
  return (
    <img src="/~icon/logo.svg" alt="" className="brand-logo" width={100} height={100} />
  );
}
